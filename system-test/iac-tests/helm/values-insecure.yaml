# IAC-13: Helm Chart Security — latest tag, missing securityContext
# Test: TC-IAC-016
image:
  repository: myapp
  tag: latest

service:
  port: 80
  type: ClusterIP

ingress:
  enabled: true
  host: myapp.example.com

# securityContext not set — runs as root
# podSecurityContext not set
# readOnlyRootFilesystem not set
