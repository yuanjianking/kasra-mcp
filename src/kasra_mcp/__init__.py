"""Kasra MCP — local code review via Model Context Protocol.

Run the server::

    kasra-mcp
    # or
    python -m kasra_mcp.server

Connects to a Kasra API instance (default: http://localhost:8090).
Set ``KASRA_API_URL`` env var to change the target.
"""
